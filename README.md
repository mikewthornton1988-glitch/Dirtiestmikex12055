# Dirtiestmikex12055

## Setup

Environment variables have been configured for this project.

### Using the Environment Variables

The environment variables are already set up in the `.env` file with the following keys:
- `KEY_1`: Simple key-value configuration
- `KEY_2`: Value with spaces (quoted)
- `KEY_3`: Multi-line certificate format

The `.env` file is ready to use and contains the configured values. If you need to modify the values, edit the `.env` file directly.

### For New Setup

If the `.env` file doesn't exist, copy the template:
```bash
cp .env.example .env
```

The project is now configured and ready to use.